{
  "name": "sistema-de-combate-de-robos",
  "version": "1.0.0",
  "description": "...",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Não há processo de build necessário\"" 
    // ou outro comando de build real (como "next build" ou "npm run compile")
  },
  "dependencies": {
    "axios": "^1.12.2",
    "navigate": "^0.3.6",
    "react-hot-toast": "^2.6.0",
    "react-router-dom": "^7.9.4"
  }
}
